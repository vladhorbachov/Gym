<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/bg_primary"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.gymshark.utils.view.calendarview.CalendarView
        android:id="@+id/cvCalendar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/space_screen"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>


    <include
        android:id="@+id/todayCard"
        layout="@layout/view_today_workout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/cvCalendar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvExercises"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_margin="@dimen/space_screen"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/todayCard"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/todayCard" />

    <ScrollView
        android:id="@+id/restTipsPanel"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_margin="@dimen/space_screen"
        android:background="@drawable/bg_profile_settings_panel"
        android:clipToPadding="false"
        android:fillViewport="true"
        android:overScrollMode="ifContentScrolls"
        android:padding="16dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/todayCard"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/todayCard">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvRestTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Rest day"
                android:textColor="@color/textPrimary"
                android:textSize="20sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvRestSubtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="5dp"
                android:text="Recover well and come back stronger."
                android:textColor="@color/textSecondary"
                android:textSize="13sp" />

            <TextView
                android:id="@+id/tvRestTipOne"
                style="@style/Widget.Gym.RestTip"
                android:layout_marginTop="14dp" />

            <TextView
                android:id="@+id/tvRestTipTwo"
                style="@style/Widget.Gym.RestTip" />

            <TextView
                android:id="@+id/tvRestTipThree"
                style="@style/Widget.Gym.RestTip" />

            <TextView
                android:id="@+id/tvRestTipFour"
                style="@style/Widget.Gym.RestTip" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnRestSetup"
                style="@style/Widget.Material3.Button"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_marginTop="14dp"
                android:background="@drawable/bg_profile_settings_action"
                android:text="Set weekly plan"
                android:textColor="@color/trainingOnAction"
                android:visibility="gone"
                app:backgroundTint="@null"
                app:cornerRadius="24dp" />
        </LinearLayout>
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>

